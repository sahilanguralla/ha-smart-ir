[tool.commitizen]
name = "cz_conventional_commits"
version = "0.7.0"
version_files = [
    "custom_components/rewire/manifest.json:version"
]
scopes = [
    "rewire"
]
tag_format = "$version"

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B", "I"]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
